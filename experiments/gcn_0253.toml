experiment_name = "gcn_0253"
split_name = "leave_B_Si_out"
target = "pCMC"
featurizer = "graph"
model = "gcn"
cv_strategy = "repeated_holdout"
cv_repeats = 5

[featurizer_parameters]

[parameters]
hidden_channels = 128
num_layers = 1
embedding_dim = 64
head_hidden_dim = 64
head_layers = 1
dropout = 0.1
num_outputs = 1

[training]
learning_rate = 0.001
n_epochs = 300
batch_size = 64
include_validation = true
