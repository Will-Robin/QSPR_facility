experiment_name = "gcn_0019"
split_name = "surfactant_type_v1"
target = "pCMC"
featurizer = "graph"
model = "gcn"

[featurizer_parameters]

[parameters]
hidden_channels = 64
num_layers = 2
embedding_dim = 64
head_hidden_dim = 64
head_layers = 2
dropout = 0.1
num_outputs = 1

[training]
learning_rate = 0.001
n_epochs = 300
batch_size = 64
include_validation = true
