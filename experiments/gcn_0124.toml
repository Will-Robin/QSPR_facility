experiment_name = "gcn_0124"
split_name = "leave_F_out"
target = "pCMC"
featurizer = "graph"
model = "gcn"
cv_strategy = "repeated_holdout"
cv_repeats = 5

[featurizer_parameters]

[parameters]
hidden_channels = 128
num_layers = 1
embedding_dim = 256
head_hidden_dim = 96
head_layers = 4
dropout = 0.1
num_outputs = 1

[training]
learning_rate = 0.001
n_epochs = 300
batch_size = 64
include_validation = true
