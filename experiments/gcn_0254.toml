experiment_name = "gcn_0254"
split_name = "leave_B_Si_out"
target = "pCMC"
featurizer = "graph"
model = "gcn"
cv_strategy = "repeated_holdout"
cv_repeats = 5

[featurizer_parameters]

[parameters]
hidden_channels = 64
num_layers = 3
embedding_dim = 64
head_hidden_dim = 64
head_layers = 2
dropout = 0.1
num_outputs = 1

[training]
learning_rate = 0.001
n_epochs = 300
batch_size = 64
include_validation = true
