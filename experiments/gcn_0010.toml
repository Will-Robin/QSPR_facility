experiment_name = "gcn_0010"
split_name = "surfactant_type_v1"
target = "pCMC"
featurizer = "graph"
model = "gcn"
cv_strategy = "repeated_holdout"
cv_repeats = 5

[featurizer_parameters]

[parameters]
hidden_channels = 64
num_layers = 1
embedding_dim = 256
head_hidden_dim = 96
head_layers = 1
dropout = 0.1
num_outputs = 1

[training]
learning_rate = 0.001
n_epochs = 300
batch_size = 64
include_validation = true
