experiment_name = "gin_1"

split_name = "surfactant_type_v1"
target = "pCMC"

featurizer = "graph"
model = "gin"

[parameters]
hidden_channels = 64
num_layers = 1
embedding_dim = 128
head_hidden_dim = 64
head_layers = 0
dropout = 0.1
num_outputs = 1

[training]
include_validation = true
learning_rate = 0.001
n_epochs = 300
batch_size = 64
